// This file is required by angular.json when using the build or serve targets.
// It's also required by the bootstrap for the app but it'll get overridden if you build a
// custom build target.

import "zone.js"; // Included in TypeScript by default.
